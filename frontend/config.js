/**
 * Frontend → Backend connection config.
 *
 * After you deploy the backend to Railway, copy the public URL
 * (e.g. https://rishtaconnect-backend.up.railway.app) and paste it below.
 *
 * Leave empty for local development — the app will auto-detect localhost:4000.
 */
window.API_URL = "";
// Example after Railway deployment:
// window.API_URL = "https://your-backend.up.railway.app";
